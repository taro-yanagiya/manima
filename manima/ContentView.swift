import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello, manima!")
                .font(.largeTitle)
        }
        .frame(width: 400, height: 300)
    }
}
